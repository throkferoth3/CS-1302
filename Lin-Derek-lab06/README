Derek Lin 811354302

Type any of these commands to compile:
    make compile
    make cat
    make all

To run:
    ./cat [file_name...]
    typ - in filename to use stdin
    if no files are spcified it uses stdin
        